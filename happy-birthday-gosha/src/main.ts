import { createApp } from "vue";
import App from "./App.vue";
import 'kneekeetah-vue-ui-kit/dist/style.css';
const app = createApp(App);
app.mount("#app");

// import GameManager from './game.ts';
//
// GameManager.start().then(() => console.log('Game Started.'));
